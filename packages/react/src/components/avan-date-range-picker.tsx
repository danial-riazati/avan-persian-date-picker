'use client';

import type { AvanDateRangePickerProps } from '../types';

/** Phase 3: from–to range picker with exact Jalali boundaries */
export function AvanDateRangePicker(_props: AvanDateRangePickerProps) {
  throw new Error(
    '@avan/react: AvanDateRangePicker not implemented yet — see docs/PHASES.md Phase 3',
  );
}
